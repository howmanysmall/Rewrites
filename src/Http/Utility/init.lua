return{
	Base64 = require(script.Base64);
	CaseInsensitiveTable = require(script.CaseInsensitiveTable);
	FastSpawn = require(script.FastSpawn);
	MimeTypes = require(script.MimeTypes);
	StringPlus = require(script.StringPlus);
	Url = require(script.Url);
	t = require(script.t);
}